# Agent Instructions

## Repo Basics
- Root: `c:\Users\Albin Rodriguez\Desktop\sermon`
- Package manager: `pnpm`
- Workspace: `pnpm-workspace.yaml`

## Common Commands
- Install: `pnpm install`
- Dev (web): `pnpm --filter web dev`
- Build: `pnpm -r build`
- Test: `pnpm -r test`

## Conventions
- Prefer `rg` for search.
- Keep edits ASCII-only unless the file already uses Unicode.
- Avoid large rewrites; make minimal, targeted changes.

## Notes
- If instructions conflict, the user’s latest request wins.
